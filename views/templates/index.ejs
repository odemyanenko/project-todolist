<%- include('../templates/includes/head.ejs') %>
    <link rel="stylesheet" href="/css/listNote.css">
    </head>
<body>

<%- include('../templates/includes/nav.ejs') %>

<main>
    <% if (notes_all.length>0) { %>
    <div class="grid">
        <% for (let notes of notes_all) {%>
                 <div class="note-box">
                     <h4 class="title"><%= notes.title %></h4>
                     <div class="text"><%= notes.description %></div>
                      <div class="note-box_actions">
                      <form action="api/notes/<%= notes._id %>" method="POST">
                         <!--<input type="hidden" value="<%= notes._id %>" name="noteId"/>-->
                          <button class="del-icon" type="submit">x</button>
                      </form>
                      <a href="/notes/<%= notes._id %>" class="btn">Details</a>
                      <a href="/addNote/add-note/<%= notes._id %>?edit=true" class="btn">Edit</a>
                   </div>
              </div>
        <% } %>
    </div>
    <% } else {%>
    <h1>No Notes Found!</h1>
    <% } %>
</main>

<section>
    <% if (lists_all.length>0) { %>
    <div class="grid">
        <% for (let list of lists_all) { %>
             <div class="note-box">
               <h4 class="title"><%= list.title %></h4>
               <div class="note-box_actions">
                 <form action="/add-to-cart" method="POST">
                     <button class="del-icon">x</button>
                 </form>
               </div>
             </div>
        <% } %>
    </div>
    <% } else {%>
    <h1>No List Found!</h1>
    <% } %>
</section>
<%- include('../templates/includes/end.ejs') %>